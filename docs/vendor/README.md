# Vendored third-party files

Served with the IDE so the GitHub Pages site needs nothing from other hosts.

| Directory | Source | Licence |
| --- | --- | --- |
| `clang-wasm/` | npm `microbit-clang-wasm@21.11.0-alpha.1` (`gen/`): Clang/LLD, newlib-nano and libc++ as WebAssembly | Apache-2.0 with LLVM exception, newlib notices, ISC for `bundle.js`; see `clang-wasm/LICENSES` |
| `codemirror/` | CodeMirror 5.65.16 (cdnjs build) | MIT, `codemirror/LICENSE` |
| `fonts/` | Inter and JetBrains Mono, latin and latin-ext subsets from Google Fonts | SIL OFL 1.1, `fonts/*-OFL.txt` |
